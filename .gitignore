# Generated by Cargo — build artifacts, compiled binaries, incremental build cache
/target/

# Cargo.lock: for a *binary* (which tcp_parser is, not a library), it's recommended
# to commit this — it pins exact dependency versions for reproducible builds.
# Uncomment the line below only if this were a library crate instead.
# Cargo.lock

# Backup files some editors/tools generate
*.orig
*.bak

# OS-specific junk
.DS_Store
Thumbs.db

# Editor/IDE folders
.vscode/
.idea/
*.swp
*.swo
WARP.md

# Environment/secrets — good habit even if nothing sensitive yet
.env
.env.local

# rust-analyzer cache
.rust-analyzer/
